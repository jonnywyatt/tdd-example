const { names } = require("./names");
describe("names", () => {
  it('if you give it two names with the same letter it returns a message saying "..."', () => {});

  // if you give it two names with different letters it returns a message saying ".."
});
